# Icons

Add two PNGs here before deploying:

- `icon-192.png` — 192×192
- `icon-512.png` — 512×512

Both are referenced from `/public/manifest.json`. Use a solid background (the
manifest declares `purpose: "any maskable"` so Android will crop into a circle).
